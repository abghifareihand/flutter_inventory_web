import 'package:flutter/material.dart';

class ProfilePageKaryawan extends StatelessWidget {
  const ProfilePageKaryawan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Welcome to Profile Page Karyawan',
        style: TextStyle(
          fontSize: 24,
        ),
      ),
    );
  }
}
