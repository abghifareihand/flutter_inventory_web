import 'package:flutter/material.dart';

class HomePageKaryawan extends StatelessWidget {
  const HomePageKaryawan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Welcome to Home Page Karyawan',
        style: TextStyle(
          fontSize: 24,
        ),
      ),
    );
  }
}
