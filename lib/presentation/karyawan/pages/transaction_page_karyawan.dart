import 'package:flutter/material.dart';

class TransactionPageKaryawan extends StatelessWidget {
  const TransactionPageKaryawan({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Welcome to Transaction Page Karyawan',
        style: TextStyle(
          fontSize: 24,
        ),
      ),
    );
  }
}
